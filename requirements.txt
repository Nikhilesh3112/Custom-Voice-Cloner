# Core dependencies
streamlit==1.28.0
numpy==1.24.0
scipy==1.10.0
scikit-learn==1.3.0

# Audio processing
librosa==0.10.0
soundfile==0.12.1
sounddevice==0.4.6
PyAudio==0.2.13

# Machine Learning
tensorflow==2.13.0

# Other utilities
python-dotenv==1.0.0
SpeechRecognition==3.10.0
pydub==0.25.1

# Explicitly exclude problematic packages
# Use the built-in wave module instead of the PyPI package
# mysql-python is not compatible with Python 3.x
